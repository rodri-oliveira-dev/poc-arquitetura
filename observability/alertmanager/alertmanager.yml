global:
  resolve_timeout: 5m

route:
  receiver: local-ui
  group_by:
    - alertname
    - service
    - job
  group_wait: 10s
  group_interval: 5m
  repeat_interval: 1h

receivers:
  - name: local-ui
